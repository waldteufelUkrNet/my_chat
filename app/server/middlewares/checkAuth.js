// поки що модуль відключив: не плануюю багатосторінковий чат, відповідно не
// буде переходів на сторінки і не треба постійно перевіряти авторизацію.
// Достатньо авторизації при старті.

module.exports = function(req,res,next){
  if(!req.session.user) {
    // next(createError(403, '403: Доступ заборонено'));
  } else {
    next();
  }
}